ru:
  activerecord:
    models:
      counter:
        one: показания
        other: показания
    validation:
      message1: "должна существовать запись с датой, ранее чем введенная"
      message2: "новые показания должны превышать предыдущее значение %{p1}"
    attributes:
         counter:
           warm: Горячая

  helpers:
    submit:
      create: Новые %{model}
      submit: Сохранить %{model}
      update: Сохранить %{model}
